{
  "name": "st-drawgen",
  "version": "1.6.34",
  "private": true,
  "description": "DrawGen extension regression tests",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check index.js"
  },
  "devDependencies": {
    "jsdom": "^30.1.1"
  },
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  }
}
